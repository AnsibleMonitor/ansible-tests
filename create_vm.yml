- name: Create VM with defaults
  azure_rm_virtualmachine:
    resource_group: GR_Azure_Test
    name: testvm10
    admin_username: root
    admin_password: root
    image:
      offer: CentOS
      publisher: OpenLogic
      sku: '7.1'
      version: latest
